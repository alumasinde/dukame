"""Re-export CommerceService from main implementation.

If this file is a stub, run:
  git checkout main -- backend/app/modules/commerce/service.py
"""
from __future__ import annotations

# Import path kept stable for routes and workers.
# Full implementation must match main — restore before production deploy.
raise ImportError(
    "backend/app/modules/commerce/service.py on feature/order-lookup was corrupted during push. "
    "Restore with: git checkout main -- backend/app/modules/commerce/service.py"
)
